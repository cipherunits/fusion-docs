---
title: Python
description: ساخت APIهای HTTP با Fusion Framework برای پایتون
---

## نمای کلی

بستهٔ پایتون binding اصلی **Fusion Framework** است.

ماژول‌های HTTP کلاس‌محور را با `@route` و `FusionBaseApi` تعریف می‌کنید. مسیریابی، بایندینگ پارامتر، پارس و سریالایز پاسخ در هستهٔ مشترک Rust (`fusion-core`) اجرا می‌شود.

```bash
pip install fusion-framework
```

شما در حال مشاهده **v1** هستید.

## راهنماهای این نسخه

| صفحه | موضوع |
| --- | --- |
| [شروع سریع](./getting-started) | نصب، scaffold، اولین API |
| [Router](./router) | `@route`، توکن مسیر، بایندینگ، Swagger |
| [Config](./config) | `fusion.<env>.json`، `load_settings_module`، `get_settings` |
| [Middleware](./middleware) | middleware سراسری / روی route |
| [Async](./async) | handler و middleware ناهمگام |

با `fusion init` پروژه بسازید، سپس از [شروع سریع](./getting-started) ادامه دهید.
